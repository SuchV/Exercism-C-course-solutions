#include "bob.h"
#include <string>

namespace bob {
    std::string hey(std::string ourLine) {
        bool upperFlag = 1;
        bool questionFlag = 0;
        bool silenceFlag = 0;
        for (int i = 0 ; i<=ourLine.length();i++){
            
        }
    }
}  // namespace bob
